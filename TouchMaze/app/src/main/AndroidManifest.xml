<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nf28.touchmaze">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".login.DialogHandler"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ConnectionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.InvitationActivity" />
        <activity android:name=".activity.GameMazeActivity" />
        <activity android:name=".activity.GameMapActivity"
            android:screenOrientation="landscape"/>
        <activity android:name=".activity.ChatActivity"></activity>
        <activity android:name=".activity.EnigmaExploActivity"></activity>
        <activity android:name=".activity.EnigmaGuideActivity"
            android:screenOrientation="portrait"
            />
    </application>

</manifest>